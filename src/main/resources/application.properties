spring.application.name=FeedbackAnalyzer

# Datasource configuration
spring.datasource.url=datasource.URL Here
spring.datasource.username=datasource.username Here
spring.datasource.password=datasource.password Here
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MariaDBDialect

gemini.api.key=Gemini API Here
